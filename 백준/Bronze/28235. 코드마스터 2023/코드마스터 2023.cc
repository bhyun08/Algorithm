#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    string a;
    cin>>a;
    if(a=="SONGDO") cout<<"HIGHSCHOOL";
    if(a=="CODE") cout<<"MASTER";
    if(a=="2023") cout<<"0611";
    if(a=="ALGORITHM") cout<<"CONTEST";
    return 0;
}
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int a,b;
    cin>>a>>b;
    cout << fixed;
    cout.precision(9);
    cout<<(double)a/b;
    return 0;
}
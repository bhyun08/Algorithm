#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    while(1){
    int x,y;
    cin>>x>>y;
    if(x==0&&y==0) break;
    cout<<x+y<<"\n";
    }
    return 0;
}
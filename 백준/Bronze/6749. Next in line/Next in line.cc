#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int big,small;
    cin>>small>>big;
    cout<<(big-small)+big;
    return 0;
}